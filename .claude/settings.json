{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); [ -z \"$f\" ] && exit 0; cd /home/ubuntu/newsblog && git add \"$f\" && git diff --cached --quiet && exit 0; git commit -m \"auto: update $f\" && git pull origin main --rebase -q 2>/dev/null; git push origin main -q 2>/dev/null || true",
            "statusMessage": "Auto-committing changes..."
          }
        ]
      }
    ]
  }
}
